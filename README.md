# STM32_FromScratch
A repo that set's up an environment from scratch for stm32F4 (Linker script, startup file, makefile, ....) 

I am writing an article how i start at STM32 From Scratch, you can read it here [artcile](articles/Fromscratch.md)<br>

I also created a comparison table between different STM32s [Here](./STM32_comparison.md)<br>

The [template_directory](./Template_proj) contains a more elaborate environment that is developed by ST ( `linker file` `startup file` and `libray` contains the STM32F4xx headers needed  )
